# name = spec-02-18

plain:
  This unquoted scalar
  spans many lines.

quoted: "So does this
  quoted scalar.\n"

# +STR
# +DOC
# +MAP
# =VAL :plain
# =VAL :This unquoted scalar spans many lines.
# =VAL :quoted
# =VAL "So does this quoted scalar.\n
# -MAP
# -DOC
# -STR
