# name = spec-05-12
# fail = true
# mark.line = 8
# mark.column = 11
# error = While scanning for the next token, found character '\t', index 9 that cannot start any token\n

# Tabs do's and don'ts:
# comment: 	
quoted: "Quoted		"
block: |
  void main() {
  	printf("Hello, world!\n");
  }
elsewhere:	# separation
	indentation, in	plain scalar

# null
