# name = recursive-alias
# fail = true
# mark.line = 2
# mark.column = 8
# mark2.line = 1
# mark2.column = 4
# error = Unable to load test/recursive-alias.yaml#recursive-alias:yaml: Found recursive alias: anchor\n

a: &anchor {
    b: *anchor
}

# null
